package java.features.collections;

public class _04_Collection_Methods {

    public static void main(String[] args) {

        /*
            Collection Common Methods
            =========================
            If we want to represent a group of individuals objects as a single entity, then
            we should go for Collection. Collection interface defines the most common methods
            which are applicable for any collection objects.

            boolean add(Object o)
            boolean addAll(Collection c)
            boolean remove(Object o)
            boolean removeAll(Collection c)
            boolean retainAll(Collection c)
            void clear()
            boolean contains(Object o)
            boolean containsAll(Collection c)
            boolean isEmpty()
            int size()
            Object[] toArray()
            Iterator iterator()

            Note: There is no concrete class that implements collection interface directly.
        */
    }

}
